# ─────────────────────────────────────────────────────────────
# Sanity CMS  ←  https://www.sanity.io/manage → tu proyecto
# SANITY_API_TOKEN debe tener rol "Editor" para escribir leads
# desde el chat widget; "Viewer" si el sitio solo lee contenido.
# NUNCA uses rol "Deploy Studio" en producción.
# ─────────────────────────────────────────────────────────────
SANITY_PROJECT_ID=your_project_id_here
SANITY_DATASET=production
SANITY_API_TOKEN=your_sanity_token_here   # ← PRIVADO: sin prefijo PUBLIC_

# ─────────────────────────────────────────────────────────────
# Resend (email / captura de leads)  ←  https://resend.com/api-keys
# ─────────────────────────────────────────────────────────────
RESEND_API_KEY=re_your_api_key_here       # ← PRIVADO
LEAD_RECIPIENT_EMAIL=hola@lixan.co
RESEND_FROM_EMAIL=noreply@lixan.co

# ─────────────────────────────────────────────────────────────
# OpenAI  ←  https://platform.openai.com/api-keys
# Regenerar si fue compartida en un canal inseguro.
# ─────────────────────────────────────────────────────────────
OPENAI_API_KEY=sk-proj-your_key_here      # ← PRIVADO

# ─────────────────────────────────────────────────────────────
# Site — URL pública sin trailing slash
# En local: http://localhost:4321
# En producción: https://lixan.co (o tu dominio real)
# ─────────────────────────────────────────────────────────────
PUBLIC_SITE_URL=http://localhost:4321

# ─────────────────────────────────────────────────────────────
# Cal.com  ←  https://cal.com/settings
# PUBLIC_ porque se usa en el componente de React del lado cliente
# ─────────────────────────────────────────────────────────────
PUBLIC_CAL_USERNAME=lixan
PUBLIC_CAL_EVENT_SLUG=reunion-estrategica
CAL_API_KEY=cal_live_your_key_here        # ← PRIVADO (para integraciones server-side)

# ─────────────────────────────────────────────────────────────
# NOTAS DE SEGURIDAD
# ─────────────────────────────────────────────────────────────
# • Variables SIN prefijo PUBLIC_ nunca llegan al navegador.
# • Variables CON prefijo PUBLIC_ son visibles en el bundle del cliente.
#   Úsalas solo para valores que es seguro exponer (IDs, slugs, URLs).
# • Configura CORS en https://www.sanity.io/manage → API → CORS origins:
#   - https://lixan.co  (producción)
#   - http://localhost:4321  (desarrollo)
# • El SANITY_API_TOKEN de producción debe ser de tipo "Editor",
#   NUNCA "Deploy Studio".
